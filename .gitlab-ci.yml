stages:
  - 🤞tests
  - 📦build

# === Some tests ===
🤔function_tests:
  stage: 🤞tests
  tags:
    - cc-runner
  only:
    - merge_requests  
  script:
    - ls
    - apt install snapd -y


#test:
#  image: golang
#  script:
#    - go test -v ./...
