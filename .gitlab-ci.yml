stages:
  - 🤞tests
  - 📦build

# === Some tests ===
🤔function_tests:
  stage: 🤞tests
  tags:
    - cc-runner
  only:
    - merge_requests  
  script:
    - go version
    - go test -v ./...


#test:
#  image: golang
#  script:
#    - go test -v ./...
